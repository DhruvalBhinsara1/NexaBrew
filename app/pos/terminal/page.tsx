import { PosTerminal } from "@/components/pos/PosTerminal";

// Full POS terminal — data fetching, cart state, and realtime handled client-side.
export default function PosTerminalPage(): React.ReactElement {
  return <PosTerminal />;
}
